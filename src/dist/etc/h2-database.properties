# Configures SQL persistence to use a local H2 database.  This is suitable for testing, but is
# not replicated, and therefore not recommended for a production environment.
jdbcUrl=jdbc:h2:/var/db/aurora-h2;MODE=MYSQL
